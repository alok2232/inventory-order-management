# PostgreSQL credentials — copy to .env and customise
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=inventory_db

# For local dev (non-Docker), set this to point directly at the backend
# VITE_API_URL=http://localhost:8000
# In Docker Compose this stays as /api (nginx proxies it)
VITE_API_URL=/api
